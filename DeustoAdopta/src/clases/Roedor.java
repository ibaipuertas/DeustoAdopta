package clases;

import javax.swing.ImageIcon;

public class Roedor extends Animal{

	public Roedor(String nombre, String tipo, int edad, ImageIcon foto, Genero genero, Usuario propietario) {
		super(nombre, tipo, edad, foto, genero, propietario);
		
	}
	
	
}
