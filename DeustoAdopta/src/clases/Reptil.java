package clases;

import javax.swing.ImageIcon;

public class Reptil extends Animal{

	public Reptil(String nombre, String tipo, int edad, ImageIcon foto, Genero genero, Usuario propietario) {
		super(nombre, tipo, edad, foto, genero, propietario);
		
	}

	
}
